{
    "private": true,
    "name": "@lambda/admin-service",
    "description": "The admin microservice for the sputnik",
    "main": "libs.js",
    "version": "0.0.0",
    "scripts": {
        "build": "yarn parcel build index.js --target=node --global handler -o dist/index.js --bundle-node-modules",
        "postbuild": "mkdir -p ../../dist/@lambda && cd dist && zip ../../../dist/$npm_package_name.zip *"
    },
    "dependencies": {
        "moment": "2.24.0",
        "underscore": "1.10.2"
    }
}
