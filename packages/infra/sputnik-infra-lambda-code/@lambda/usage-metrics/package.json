{
    "private": true,
    "name": "@lambda/usage-metrics",
    "version": "0.0.0",
    "description": "Usage metrics is an auxiliary class to capture metrics pertinent for feedback on the solution",
    "main": "metrics.common.js",
    "scripts": {
        "build": "yarn parcel build metrics.common.js --target=node --global handler -o dist/index.js --bundle-node-modules"
    },
    "dependencies": {
        "moment": "2.24.0"
    },
    "devDependencies": {
        "aws-sdk": "*",
        "chai": "*",
        "sinon": "*",
        "sinon-chai": "*",
        "mocha": "*",
        "aws-sdk-mock": "*",
        "npm-run-all": "*",
        "proxyquire": "*"
    }
}
