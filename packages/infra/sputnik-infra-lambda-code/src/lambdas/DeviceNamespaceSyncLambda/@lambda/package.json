{
    "private": true,
    "name": "@lambda/device-namespace-sync",
    "main": "index.js",
    "version": "0.0.1",

    "scripts": {
        "build": "mkdir -p ../../../../dist/@lambda && zip -FSrq ../../../../dist/$npm_package_name.zip *"
    },
    "devDependencies": {
        "aws-sdk": "*",
        "@lambda/sputnik-lib": "^0.0.1",
        "aws-appsync": "^3.0.3",
        "cross-fetch": "^3.0.4",
        "graphql-tag": "^2.10.3"
    },
    "peerDependencies": {
        "@lambda/npm-dependencies": "0.0.1",
        "@lambda/sputnik-lib": "0.0.1",
        "aws-appsync": "^3.0.3",
        "cross-fetch": "^3.0.4",
        "graphql-tag": "^2.10.3"
    }
}
