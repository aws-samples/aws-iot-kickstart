{
    "private": true,
    "name": "@lambda/s3-helper",
    "description": "Cloudformation Custom Resource Helper for S3",
    "main": "index.js",
    "version": "0.0.1",
    "scripts": {
        "build": "mkdir -p ../../../../dist/@lambda && zip -FSrq ../../../../dist/$npm_package_name.zip *"
    },
    "peerDependencies": {
        "@lambda/npm-dependencies": "0.0.1",
        "@lambda/sputnik-lib": "0.0.1",
        "aws-sdk": "*",
        "moment": "2.24.0",
        "underscore": "1.10.2"
    }
}
