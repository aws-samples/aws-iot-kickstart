query ListDeployments($limit: Int, $nextToken: String) {
  listDeployments(limit: $limit, nextToken: $nextToken) {
    deployments {
      thingId
      deploymentId
      type
      createdAt
      updatedAt
    }
    nextToken
  }
}
