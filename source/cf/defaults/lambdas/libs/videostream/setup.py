from setuptools import setup

setup(name='videostream',
      version='0.1',
      description='camera code',
      author='teuteuguy',
      license='MIT',
      packages=['videostream'],
      zip_safe=False)
