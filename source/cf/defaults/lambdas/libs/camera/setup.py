from setuptools import setup, find_packages

setup(name='camera',
      version='0.1',
      description='camera code',
      author='teuteuguy',
      license='MIT',
      packages = find_packages(),
      zip_safe=False)
